# Cloudflare Workers URL
# For development, we recommend using the deployed backend to avoid CORS issues
VITE_SOCKET_URL=https://scrum-poker.rivera-family.workers.dev

# For local development with workers running locally (may have CORS issues)
# VITE_SOCKET_URL=http://localhost:8787

# For production, this will be automatically detected from the current domain
# or you can set it explicitly:
# VITE_SOCKET_URL=https://scrum-poker.rivera-family.workers.dev

# Optional: Custom port for Vite dev server
# PORT=5173 